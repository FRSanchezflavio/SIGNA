import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const InteligenciaAvanzada: React.FC = () => {
  return (
    <div>
      <Title level={2}>Módulo de Inteligencia Avanzada (MIA)</Title>
      <p>Análisis predictivo, correlaciones y comparación de períodos</p>
    </div>
  );
};

export default InteligenciaAvanzada;
